"""LLM provider abstraction and factory.

The factory and provider classes live in submodules — import directly:

    from evalforge.providers.factory import make_provider, SUPPORTED_PROVIDERS
    from evalforge.providers.base import GeneratedCase, LLMProvider
"""
